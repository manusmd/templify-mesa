name: Build and deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    permissions:
      contents: write
      packages: write
    uses: manusmd/github-workflows/.github/workflows/build-and-deploy.yml@main
    with:
      app-name: templify-mesa
